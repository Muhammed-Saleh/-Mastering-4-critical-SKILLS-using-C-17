#include <iostream>
using namespace std;

int main() {
	int A, B;
	cin >> A;
	B = A;
	cin >> A;

	if ( B < A ) {
		B = A;
	}

	cin >> A;

	if ( B < A ) {
		B = A;
	}

	cin >> A;

	if ( B < A ) {
		B = A;
	}

	cin >> A;

	if ( B < A ) {
		B = A;
	}

	cin >> A;

	if ( B < A ) {
		B = A;
	}

	cin >> A;

	if ( B < A ) {
		B = A;
	}

	cin >> A;

	if ( B < A ) {
		B = A;
	}

	cin >> A;

	if ( B < A ) {
		B = A;
	}

	cin >> A;

	if ( B < A ) {
		B = A;
	}

	cout << B << endl;
	return 0;
}
